<Window x:Class="TeeKay87.MemoryEngine.App.Dialogs.MemoryEditDialog"
        Icon="/TeeKay87.MemoryEngine;component/Assets/TK87ME.ico"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="660"
        MinWidth="600"
        MaxWidth="900"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}"
        Loaded="MemoryEditDialog_Loaded">
    <Border Margin="18"
            Padding="18"
            Style="{StaticResource CardBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="8" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="8" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="1" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Text="Edit Hex Bytes"
                           Style="{StaticResource SectionTitleTextStyle}" />
                <TextBlock x:Name="TargetTextBlock"
                           Margin="0,4,0,0"
                           Style="{StaticResource MutedTextStyle}" />
            </StackPanel>

            <TextBlock Grid.Row="2"
                       Text="Current bytes"
                       FontWeight="SemiBold" />
            <TextBox x:Name="CurrentBytesTextBox"
                     Grid.Row="4"
                     IsReadOnly="True"
                     FontFamily="Consolas"
                     HorizontalScrollBarVisibility="Auto" />

            <TextBlock Grid.Row="6"
                       Text="New bytes"
                       FontWeight="SemiBold" />
            <TextBox x:Name="NewBytesTextBox"
                     Grid.Row="8"
                     FontFamily="Consolas"
                     HorizontalScrollBarVisibility="Auto"
                     ToolTip="Enter hexadecimal bytes, for example: 90 90 90 90" />

            <Border Grid.Row="10"
                    Padding="10,8"
                    Background="{DynamicResource AccentMutedBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                <TextBlock x:Name="SafetyTextBlock"
                           TextWrapping="Wrap"
                           Style="{StaticResource MutedTextStyle}" />
            </Border>

            <TextBlock x:Name="ValidationTextBlock"
                       Grid.Row="12"
                       Foreground="{DynamicResource ErrorTextBrush}"
                       TextWrapping="Wrap" />

            <Border Grid.Row="14"
                    Height="1"
                    Background="{DynamicResource BorderBrush}" />

            <StackPanel Grid.Row="16"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                <Button MinWidth="96"
                        Style="{StaticResource DangerButtonStyle}"
                        IsCancel="True"
                        Content="Cancel" />
                <Button MinWidth="122"
                        Margin="8,0,0,0"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="WriteButton_Click"
                        Content="Write &amp; Verify" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
